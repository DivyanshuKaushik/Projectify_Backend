{
    "version": 2,
    "public": true,
    "buildCommand":"npm run build",
    "builds": [
        {
            "src": "dist/app.js",
            "use": "@now/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/dist/app.js"
          }
    ]
}